# Knowledge Base Overview

**Domain:** Software Engineering Knowledge — patterns, techniques, frameworks, languages

**Status:** Newly initialized. No sources ingested yet.

**Purpose:** This wiki stores reusable development knowledge (patterns, techniques, frameworks) that the coding agent consults during implementation tasks. Raw sources live in `raw/` (human-curated, immutable). The wiki in `wiki/` is LLM-maintained.

**Next step:** Drop a source file into `raw/` and run: `ingest raw/[filename]`
